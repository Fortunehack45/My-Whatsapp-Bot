[phases.setup]
nixPkgs = ["ffmpeg", "python311", "yt-dlp"]

[phases.install]
# Do NOT use --production — we need all dependencies
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'No build step required'"]

[start]
cmd = "npm start"
